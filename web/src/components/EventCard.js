import { urlencoded } from 'body-parser'
import React from 'react'
import './event_card.css'
function EventCard({name,link,photo,content}) {
    return (
        <div className="event-container">
        </div>
    )
}

export default EventCard
